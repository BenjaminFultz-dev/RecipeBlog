<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
       <img src="/img/foodPic.jfif" width="600" height="500" class="d-block mx-lg-auto img-fluid rounded" Loading="lazy" alt="Cooking Blog home image"> 
    </div>
    <div class="col-12 col-lg-6">
        <h2 class="display-5 fw-bold mb-3">Ever-expanding myriad of delicious recipe ideas</h2>
        <p class="lead">Lorem ipsum dolor sit amet consectetur, adipisicing elit. Magnam ea illo, adipisci voluptatem beatae eaque magni, modi maiores dolore deserunt ab mollitia nulla nesciunt cum libero unde distinctio autem harum.</p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Random Recipe</a>
        </div>
    </div>
</div>

<!--CATEGORIES-->

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">
    <% if (categories != '') { %>
        <% categories.forEach(function(category, index) { %>
            <a href="/categories/<%- category.name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="img/<%- category.image %>" alt="<%- category.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
        <% }) %>
    <% } %>
 

    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="img/foodpic8.jfif" alt="View All Categories" Loading="lazy">
        </div>
        <div class="pt-1">View All</div>
    </a>
</div>

<!--LATEST-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if (food != '') { %>
            <% food.latest.forEach(function(recipe, index){ %>
            <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                <div class="category__img category__img--large shadow">
                    <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%= recipe.name %></div>
            </a>
            <% }) %>
        <% } %>

    </div>
</section>

<!--APPETIZERS-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Appetizers</h2>
        <a href="/categories/Appetizers" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if (typeof food.appetizers !== 'undefined' && food.appetizers.length > 0) { %>
            <% food.appetizers.forEach(function(recipe, index){ %>
            <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                <div class="category__img category__img--large shadow">
                    <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%= recipe.name %></div>
            </a>
            <% }) %>
            <% } else { %>
                <p>No items found.</p>
        <% } %>

    </div>
</section>

<!--BREAKFAST-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Breakfast</h2>
        <a href="/categories/Breakfast" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if (typeof food.breakfast !== 'undefined' && food.breakfast.length > 0) { %>
            <% food.breakfast.forEach(function(recipe, index){ %>
            <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                <div class="category__img category__img--large shadow">
                    <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%= recipe.name %></div>
            </a>
            <% }) %>
            <% } else { %>
                <p>No items found.</p>
        <% } %>

    </div>
</section>

<!--LUNCH-->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Lunch</h2>
        <a href="/categories/Lunch" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

        <% if (typeof food.lunch !== 'undefined' && food.lunch.length > 0) { %>
            <% food.lunch.forEach(function(recipe, index){ %>
            <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                <div class="category__img category__img--large shadow">
                    <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" Loading="lazy">
                </div>
                <div class="pt-1"><%= recipe.name %></div>
            </a>
            <% }) %>
            <% } else { %>
                <p>No items found.</p>
        <% } %>

    </div>
</section>




<!--SUBMIT-->

<section class="px-4 py-5 my-5 text-center">
    <img src="/img/foodPic6.jfif" alt="Publish your recipe" class="d-block mx-auto mb-4 img-fluid" width="566" height="208" Loading="lazy">
    <h2 class="display-5 fw-bold">Publish Your Recipe</h2>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your recipe and share your delicious ideas with the world!</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
           <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a> 
        </div>
    </div>
</section>